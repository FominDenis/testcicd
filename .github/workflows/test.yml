name: Test

on:
  schedule:
    - cron: '27 6-12 * * 0-4'

jobs:
  test-job:
    name: Test Step
    if: ${{ github.event.pull_request.user.login == 'FominDenis' }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo -e "test!"